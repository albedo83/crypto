{
  "_comment": "'path' is the nginx subpath for each bot. Bot 2 has no path because no nginx location is configured for it — Open button falls back to host:port (direct-access only).",
  "bots": [
    {"port": 8097, "label": "Paper",   "mode": "paper", "path": "/paper"},
    {"port": 8098, "label": "Live",    "mode": "live",  "path": "/bot"},
    {"port": 8099, "label": "Junior",  "mode": "paper"}
  ]
}
